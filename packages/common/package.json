{
  "name": "@coffee-shop/common",
  "private": true,
  "main": "src/main.ts",
  "scripts": {
    "build": "swc --config-file ../../.swcrc.common src --out-dir dist",
    "serve": "node dist/main.js",
    "lint": "eslint . --ext .ts,.tsx --fix",
    "test": "jest"
  }
}

{
  "name": "@cs/common",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "swc --config-file ../../.swcrc src --out-dir dist",
    "serve": "node dist/main.js",
    "lint": "eslint . --ext .ts,.tsx --fix",
    "test": "jest"
  }
}

{
  "name": "@cs/backend",
  "version": "0.0.1",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc --project ./tsconfig.build.json",
    "serve": "node dist/index.js",
    "test": "vitest run",
    "test:cov": "vitest run --coverage"
  },
  "dependencies": {
    "@cs/common": "*"
  }
}

{
  "name": "@coffee-shop/backend",
  "private": true,
  "main": "src/main.ts",
  "scripts": {
    "build": "swc src --out-dir dist",
    "serve": "node dist/main.js",
    "lint": "eslint . --ext .ts,.tsx --fix",
    "test": "jest"
  }
}
